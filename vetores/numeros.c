#include <stdio.h>

int main()
{
    //vetor com numeros declarados no inicio
    //tudo na computaçao começa do0 (zero)
    // o vetor trabalha com as posições
    // 0->4, 1->9, 2->12,3->3
    int numeros[] = {4,9,12,3};

    printf("numero na posicao 2: %i", numeros[2]);

    return 0;
}
